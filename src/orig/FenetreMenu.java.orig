import javax.swing.*;
import java.awt.*;
import java.awt.event.*;
import javax.swing.event.*;

public class FenetreMenu extends FenetreModele
{
        JPanel pAction = new JPanel();
        JButton bJeu = new JButton("Jouer");
        JButton bScore = new JButton("Score");
        JButton bQuitter = new JButton("Quitter");
        JMenuItem mReset;
        JMenuItem mJouer;       
    //bQuitter.addActionListener(new ActionQuitter());

        /*fMenu.pack();
        fMenu.setVisible(true);*/

        public FenetreMenu()
        {
            super("Calcul Menu - Menu");
            bQuitter.addActionListener(new ActionQuitter());
            mJouer = new JMenuItem("Jouer");
        mJouer.setFont(fCambMini);
        
        mJeu.add(mJouer);

            pAction.add(bJeu);
            pAction.add(bScore);
            pAction.add(bQuitter);
            fenetre.add("South",pAction);
            fenetre.pack();
            fenetre.setVisible(true);

        }
}
